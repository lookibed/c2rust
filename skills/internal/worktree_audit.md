# Worktree audit

Create a dedicated Git worktree (`git worktree add`) for mutation-based audit. Do not mutate the
implementation worktree. Record worktree path, narrow command, restoration command,
and clean `git diff` proof.
